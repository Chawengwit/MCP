{
  "apis": {
    "github": {
      "type": "rest",
      "base_url": "https://api.github.com",
      "auth": {
        "type": "oauth2",
        "provider": "github",
        "client_id": "${GITHUB_CLIENT_ID}",
        "client_secret": "${GITHUB_CLIENT_SECRET}",
        "authorize_url": "https://github.com/login/oauth/authorize",
        "token_url": "https://github.com/login/oauth/access_token",
        "redirect_uri": "http://127.0.0.1:8765/callback",
        "scopes": ["read:user", "public_repo"]
      },
      "endpoints": {
        "get_user": {
          "method": "GET",
          "path": "/user"
        },
        "list_repos": {
          "method": "GET",
          "path": "/user/repos",
          "query_params": ["sort", "per_page"]
        }
      },
      "logging": {
        "request_payload": "metadata",
        "response_payload": "summary",
        "redact_fields": ["email", "notification_email"]
      },
      "limits": {
        "timeout_seconds": 30,
        "max_retries": 3
      }
    },
    "taximail": {
      "type": "rest",
      "base_url": "https://api.taximail.com",
      "auth": {
        "type": "session_login",
        "login_path": "/v2/user/login",
        "login_method": "POST",
        "login_content_type": "application/x-www-form-urlencoded",
        "credentials": {
          "api_key": "{api_key}",
          "secret_key": "{secret_key}"
        },
        "session_id_field": "data.session_id",
        "session_expire_field": "data.expire",
        "user_id_field": "_api_key_fingerprint",
        "session_header": "Authorization",
        "session_format": "Bearer {session_id}"
      },
      "endpoints": {
        "list_subscribers": {
          "method": "GET",
          "path": "/v2/list/35/subscribers",
          "query_params": ["display_mode", "order_by", "order_type", "page", "limit"],
          "description": "List subscribers in Taximail mailing list 35. Returns subscriber email, status, and metadata.",
          "required_params": ["display_mode"],
          "param_hints": {
            "display_mode": "REQUIRED. Subscriber status filter. Valid values: 'all', 'active', 'unsubscribed', 'bounce', 'spam'. Use 'all' to see every subscriber regardless of status.",
            "order_by": "Sort field. Common values: 'email', 'subscribe_date'.",
            "order_type": "Sort direction. Valid values: 'asc', 'desc'.",
            "page": "1-indexed page number for pagination. Defaults to 1.",
            "limit": "Max subscribers per page. Defaults to API default. Use 3-10 for previews."
          }
        }
      },
      "logging": {
        "request_payload": "metadata",
        "response_payload": "summary",
        "redact_fields": ["email", "subscriber_email"]
      },
      "limits": {
        "timeout_seconds": 30,
        "max_retries": 3
      }
    }
  }
}
